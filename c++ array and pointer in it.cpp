#include<iostream>
using namespace std;
int main()
{
            int *p;
            int n;
           
            cout<<"enter the size of array : ";
            cin>> n;
             p = new int[n];
             cout<<"enter elements of array : ";
             for(int i = 0 ; i <= n-1 ; i++)
             {
                cin>>p[i];
             }
             cout<<"the elements of array : ";
             for(int i = 0 ; i <= n-1 ; i++)
             {
                cout<<p[i]<<" ";
             }
             cout<<"what the element wants ot delete : "<<endl;
             for(int i = d-1 ;i<n ; i++)
             {
                
             }
}